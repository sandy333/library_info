<%= form_for(@library_info) do |f| %>
  <% if @library_info.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@library_info.errors.count, "error") %> prohibited this library_info from being saved:</h2>

      <ul>
      <% @library_info.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<%= show_tx %>


  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :address %><br />
    <%= f.text_area :address %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
